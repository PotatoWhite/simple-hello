plugins {
    id 'org.springframework.boot' version '2.4.4'
    id 'io.spring.dependency-management' version '1.0.11.RELEASE'
    id 'java'
}

group = 'me.potato'
version = '0.0.1-SNAPSHOT'
def buildNumber = System.getenv( 'BUILD_NUMBER' ).toString()
def dockerHubUser = System.getenv( 'USERNAME' ).toString()
def dockerHubPassword = System.getenv( 'PASSWORD' ).toString()

sourceCompatibility = '11'

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

test {
    useJUnitPlatform()
}
//
//bootBuildImage {
//    imageName = "bravopotato/${project.name}:$buildNumber"
//    publish = true
//    docker {
//        publishRegistry {
//            username = "$dockerHubUser"
//            password = "$dockerHubPassword"
//            email = "bravopotato@gmail.com"
//            url = "https://hub.docker.com/bravopotato"
//        }
//    }
//}